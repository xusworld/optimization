#pragma once

#include <iostream>
#include <vector>
#include <random>
#include <functional>

using namespace std;


#define A(i,j) a[ (i)*sa + (j) ]
#define B(i,j) b[ (i)*sb + (j) ]
#define C(i,j) c[ (i)*sc + (j) ]

int sa;
int sb;
int sc;
